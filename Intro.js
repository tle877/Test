import React from "react"

export default function Intro(props){
    return (
        <div>
            <h1>Hello</h1>
            <button onClick={props.isStart}>a</button>
        </div>
    )      
}